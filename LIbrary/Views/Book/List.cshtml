
@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

@if(ViewBag.Books.Count > 0)
{
    @foreach(Book book in ViewBag.Books)
    {
        <li><a href="/Book/Details">@(book.Title) by @(book.Author)</a></li>
    }
}